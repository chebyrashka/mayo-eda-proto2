{
  "name": "prototype2",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "python3 -m http.server 4330 --bind 127.0.0.1 --directory public",
    "check": "node --check public/app.js && node --check public/particles.js",
    "build": "npm run check && node scripts/build.mjs",
    "preview": "python3 -m http.server 4330 --bind 127.0.0.1 --directory dist"
  }
}
